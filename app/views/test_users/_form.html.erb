<%= form_with(model: test_user, local: true) do |form| %>
  <% if test_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_user.errors.count, "error") %> prohibited this test_user from being saved:</h2>

      <ul>
      <% test_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
